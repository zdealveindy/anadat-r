# This is my first try of using R

1+1
sqrt (25)
a <- sqrt (25)   # assign the result of squareroot calculation into var. a

plot (dist ~ speed, data = cars)
plot (dist ~ speed, data = cars, main = 'Dependence of distance on speed')  # this is a plotting function with main header
